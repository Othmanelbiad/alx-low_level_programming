#include "main.h"
/**
 * main - Entry point
 * Discription: 'Check for number if it is positive or negative'
 * Return: always 0
 */

int main(void)
{
	char str[] = "_putchar";
	int ch;

	for (ch = 0; ch < 8; ch++)
		_putchar(str[str]);
	_putchar('\n');

	return (0);
}
